<div class="max-w-[800px] mx-auto px-6 py-16">
  <h1 class="font-display text-4xl font-semibold text-[#1a1a18] mb-8">{{ page.title }}</h1>
  <div class="prose text-sm text-[#5a5a52] leading-relaxed">{{ page.content }}</div>
</div>
