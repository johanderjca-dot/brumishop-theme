<section class="section-pad benefits-section" style="background: {{ section.settings.bg_color }}">
  <div class="page-width">
    {%- if section.settings.eyebrow != blank or section.settings.heading != blank -%}
      <div class="cat-head">
        {%- if section.settings.eyebrow != blank -%}
          <p class="cat-head__eyebrow">{{ section.settings.eyebrow }}</p>
        {%- endif -%}
        {%- if section.settings.heading != blank -%}
          <h2 class="cat-head__title">{{ section.settings.heading }}</h2>
        {%- endif -%}
      </div>
    {%- endif -%}

    <div class="features__wrap">
      <div class="features__media">
        {%- if section.settings.video_alpha_url != blank -%}
          {%- comment -%} dos formatos para que el fondo transparente funcione
            en todos lados: HEVC/.mov es lo que soportan Safari e iOS,
            WebM/VP9 es lo que soportan Chrome/Firefox/Edge. El navegador
            elige el que sí entiende. {%- endcomment -%}
          <video class="features__alpha-video" autoplay muted loop playsinline
            {%- if section.settings.video_alpha_poster_url != blank %} poster="{{ section.settings.video_alpha_poster_url }}"{% endif %}>
            {%- if section.settings.video_alpha_hevc_url != blank -%}
              <source src="{{ section.settings.video_alpha_hevc_url }}" type="video/quicktime">
            {%- endif -%}
            <source src="{{ section.settings.video_alpha_url }}" type="video/webm">
          </video>
        {%- elsif section.settings.video != blank -%}
          {{ section.settings.video
            | video_tag:
              image_size: '900x',
              autoplay: true,
              loop: true,
              muted: true,
              controls: false,
              playsinline: true
          }}
        {%- elsif section.settings.image != blank -%}
          <img src="{{ section.settings.image | image_url: width: 900 }}"
               srcset="{{ section.settings.image | image_url: width: 500 }} 500w, {{ section.settings.image | image_url: width: 900 }} 900w"
               sizes="(min-width: 990px) 30vw, 70vw"
               alt="{{ section.settings.heading | escape }}"
               width="{{ section.settings.image.width }}" height="{{ section.settings.image.height }}"
               loading="lazy">
        {%- else -%}
          {{ 'product-1' | placeholder_svg_tag }}
        {%- endif -%}
      </div>

      <div class="features__items">
        {%- for block in section.blocks -%}
          <div class="features__item" {{ block.shopify_attributes }}>
            <div class="features__icon">
              {%- if block.settings.icon != blank -%}
                <img src="{{ block.settings.icon | image_url: width: 128 }}"
                     alt=""
                     width="{{ block.settings.icon.width }}" height="{{ block.settings.icon.height }}"
                     loading="lazy">
              {%- else -%}
                {% render 'icon-checkmark' %}
              {%- endif -%}
            </div>
            {%- if block.settings.title != blank -%}<p class="features__title">{{ block.settings.title }}</p>{%- endif -%}
            {%- if block.settings.text != blank -%}<p class="features__desc">{{ block.settings.text }}</p>{%- endif -%}
          </div>
        {%- endfor -%}
      </div>
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Beneficios",
  "tag": "section",
  "settings": [
    { "type": "text", "id": "eyebrow", "label": "Etiqueta pequeña (opcional)" },
    { "type": "text", "id": "heading", "label": "Título", "default": "Calidad respaldada por la ciencia" },
    { "type": "image_picker", "id": "image", "label": "Imagen central" },
    { "type": "video", "id": "video", "label": "Video central (opcional)", "info": "Si lo subes, reemplaza a la imagen. Se reproduce sin sonido y en bucle." },
    { "type": "text", "id": "video_alpha_url", "label": "URL de video .webm con fondo transparente (opcional)", "info": "Para Chrome, Firefox y Edge. Shopify no soporta estos videos si los subes aquí arriba, porque los vuelve a codificar y pierde la transparencia. Sube el archivo a Configuración → Archivos (no aquí), copia su enlace y pégalo en este campo. Si lo llenas, reemplaza a todo lo demás." },
    { "type": "text", "id": "video_alpha_hevc_url", "label": "URL de video .mov (HEVC) con fondo transparente (opcional)", "info": "Para Safari e iPhone/iPad, que no soportan el .webm de arriba. Sube el archivo .mov a Configuración → Archivos y pega su enlace aquí." },
    { "type": "text", "id": "video_alpha_poster_url", "label": "Imagen mientras carga el video (URL, opcional)", "info": "Se muestra un instante mientras el video se descarga." },
    { "type": "color", "id": "bg_color", "label": "Color de fondo", "default": "#FBF6EE" }
  ],
  "blocks": [
    { "type": "beneficio", "name": "Beneficio", "settings": [
      { "type": "image_picker", "id": "icon", "label": "Ícono" },
      { "type": "text", "id": "title", "label": "Título", "default": "Beneficio" },
      { "type": "textarea", "id": "text", "label": "Descripción" }
    ]}
  ],
  "presets": [{
    "name": "Beneficios",
    "blocks": [
      { "type": "beneficio" }, { "type": "beneficio" }, { "type": "beneficio" },
      { "type": "beneficio" }, { "type": "beneficio" }, { "type": "beneficio" }
    ]
  }]
}
{% endschema %}
