{%- liquid
  assign on_sale = false
  if product.compare_at_price > product.price
    assign on_sale = true
  endif

  assign is_bestseller = false
  for tag in product.tags
    assign tag_down = tag | downcase
    if tag_down == 'bestseller' or tag_down == 'best seller' or tag_down == 'mas vendido' or tag_down == 'más vendido' or tag_down == 'top seller'
      assign is_bestseller = true
    endif
  endfor
-%}

<a class="card" href="{{ product.url }}">
  <div class="card__media">
    {%- if product.featured_image -%}
      <img
        src="{{ product.featured_image | image_url: width: 600 }}"
        srcset="{{ product.featured_image | image_url: width: 300 }} 300w,
                {{ product.featured_image | image_url: width: 600 }} 600w,
                {{ product.featured_image | image_url: width: 900 }} 900w"
        sizes="(min-width: 990px) 25vw, 50vw"
        alt="{{ product.featured_image.alt | default: product.title | escape }}"
        width="{{ product.featured_image.width }}"
        height="{{ product.featured_image.height }}"
        loading="lazy">
    {%- else -%}
      {{ 'product-1' | placeholder_svg_tag: 'card__placeholder' }}
    {%- endif -%}

    {%- if is_bestseller -%}
      <span class="card__badge card__badge--best">Más vendido</span>
    {%- elsif on_sale and product.available -%}
      {%- assign pct = product.compare_at_price | minus: product.price | times: 100 | divided_by: product.compare_at_price -%}
      <span class="card__badge">-{{ pct }}%</span>
    {%- endif -%}

    {%- unless product.available -%}
      <span class="card__sold-out">Agotado</span>
    {%- endunless -%}
  </div>

  <div class="card__body">
    <span class="card__stars" aria-hidden="true">★★★★★</span>
    <h3 class="card__title">{{ product.title }}</h3>
    <div class="card__price">
      <strong>{{ product.price | money }}</strong>
      {%- if on_sale -%}<s>{{ product.compare_at_price | money }}</s>{%- endif -%}
    </div>
  </div>
</a>
